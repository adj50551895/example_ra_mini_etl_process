SELECT t.id,
       t.siteID,
       t.title,
       t.shortDescription,
       t.description,
       t.products,
       t.subjects,
       t.academicLevels,
       t.tags,
       t.internalTags,
       t.vanityURL,
       t.SEOUrl,
       t.heroImage,
       t.type,
       t.status,
       t.subscribed,
       t.createdAt,
       t.createdBy,
       t.updatedAt,
       t.updatedBy,
       t.publishedAt,
       t.requestID,
       t.copyLicenses,
       t.timing,
       t.ranking,
       t.technicalExpertise,
       t.standards,
       t.links,
       t.components,
       t.settings,
       t.public,
       t.locked
FROM edex_resources.Resource t;
SELECT t.id,
       t.vanityURL,
       t.siteID,
       t.title,
       t.shortDescription,
       t.description,
       t.type,
       t.courseTypeID,
       t.difficulty,
       t.assets,
       t.settings,
       t.tags,
       t.theme,
       t.workshops,
       t.contentStandards,
       t.academicLevels,
       t.products,
       t.subjects,
       t.badges,
       t.forumID,
       t.educators,
       t.relatedContent,
       t.status,
       t.enrollmentOpensAt,
       t.startsAt,
       t.enrollmentClosesAt,
       t.publishAt,
       t.closesAt,
       t.forumClosesAt,
       t.graduationAt,
       t.createdAt,
       t.createdBy,
       t.updatedAt,
       t.updatedBy,
       t.requestID,
       t.credlyBadges
FROM edex_courses.Course t;